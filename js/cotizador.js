/* eslint-disable func-names */
/* eslint-disable no-alert */
/* eslint-disable no-cond-assign */
/* eslint-disable no-inner-declarations */
/* eslint-disable no-undef */
/* eslint-disable no-shadow */
/* eslint-disable no-unused-expressions */
/* eslint-disable no-sequences */
// eslint-disable-next-line no-plusplus
document.addEventListener('DOMContentLoaded', () => { const e = document.querySelector('#nombre'); const t = document.querySelector('#apellido'); const n = document.querySelector('#email'); const s = document.querySelector('#paseDia'); const l = document.querySelector('#paseDiario'); const o = document.querySelector('#paseDosDias'); const a = document.querySelector('#regalo'); const d = document.querySelector('#calcular'); const r = document.querySelector('#lista-productos'); const i = document.querySelector('#btnRegistro'); const u = document.querySelector('#error'); const c = document.getElementById('suma-total'); const m = document.getElementById('camisa_evento'); const y = document.getElementById('etiquetas'); if (i && (i.disabled = !0), document.getElementById('viernes').style.display = 'none', document.getElementById('sabado').style.display = 'none', document.getElementById('domingo').style.display = 'none', document.getElementById('calcular')) { function p() { this.value === '' ? (u.style.display = 'block', u.innerHTML = `El campo ${this.name} es obligatorio`, this.style.border = '1px solid red', u.style.border = '1px solid red') : (u.style.display = 'none', this.style.border = '1px solid #cccccc'); } function v() { const e = parseInt(s.value, 10) || 0; const t = parseInt(o.value, 10) || 0; const n = parseInt(l.value, 10) || 0; const a = []; e > 0 ? a.push('viernes') : document.getElementById('viernes').style.display = 'none', t > 0 ? a.push('viernes', 'sabado') : document.getElementById('sabado', 'viernes').style.display = 'none', n > 0 ? a.push('viernes', 'sabado', 'domingo') : document.getElementById('domingo', 'sabado', 'viernes').style.display = 'none'; for (let e = 0; e < a.length; e++)document.getElementById(a[e]).style.display = 'block'; }d.addEventListener('click', (e) => { if (e.preventDefault(), a.value === '')alert('Debes de ingresar un regalo :)'), a.focus(); else { const e = parseInt(s.value, 10) || 0; const t = parseInt(o.value, 10) || 0; const n = parseInt(l.value, 10) || 0; const a = parseInt(m.value, 10) || 0; const d = parseInt(y.value, 10) || 0; const u = 30 * e + 45 * t + 50 * n + 10 * a * 0.93 + 2 * d; const p = []; e >= 1 && p.push(`${e} Pases por dia`), t >= 1 && p.push(`${t} Pase por dos dias`), n >= 1 && p.push(`${n} Pase completo`), a === 1 && p.push(`${a} Camisa`), d === 1 && p.push(`${d} Etiqueta`), a >= 2 && p.push(`${a} Camisas`), d >= 2 && p.push(`${d} Etiquetas`), console.log(p), r.style.display = 'block', r.innerHTML = ''; for (let e = 0; e < p.length; e++)r.innerHTML += `${p[e]}<br/>`; c.innerHTML = `$ ${u.toFixed(2)}`, i.disabled = !1, document.getElementById('total_pedido').value = u; } }), s.addEventListener('click', v), l.addEventListener('change', v), o.addEventListener('change', v), e.addEventListener('blur', p), t.addEventListener('blur', p), n.addEventListener('blur', function () { this.value.indexOf('@') > -1 && this.value.indexOf('.com') > -1 ? (u.style.display = 'none', this.style.border = '1px solid #cccccc') : (u.style.display = 'block', u.innerHTML = `El campo ${this.name} debe llevar un @ y un .com`, this.style.border = '1px solid red', u.style.border = '1px solid red'); }), document.getElementsByClassName('editar_formulario').length > 0 && (s.value || l.value || o.value) && v(), $('#paseDiario, #paseDosDias, #paseDia, #camisa_evento, #etiquetas').change(() => { i.disabled = !0; }); } });
